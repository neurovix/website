---
import Navbar from "../../components/Navbar.astro";
import Layout from "../../layouts/Layout.astro";
import Footer from "../../components/Footer.astro";
import Image from "astro/components/Image.astro";
import Hero from "../../assets/hero_supermarketml.png";
import MockupSistema1 from "../../assets/companies/superml_1.png";
import MockupSistema2 from "../../assets/companies/superml_2.png";
import SuperMarketML from "../../assets/companies/supermarket-ml.png";
---

<Layout>
  <main>
    <Navbar />

    <!-- Hero -->
    <section class="relative w-full h-screen">
      <Image class="w-full h-full object-cover" src={Hero} alt="Hero image" />
      <div class="absolute inset-0 bg-black/50 z-10"></div>
      <div
        class="px-5 lg:px-0 mt-28 absolute inset-0 flex flex-col items-center justify-center text-white text-center z-20"
      >
        <Image src={SuperMarketML} alt="Kimberly Clark Logo" />
        <h1 class="font-extrabold my-4 text-5xl">Super Market ML</h1>
        <h1 class="text-3xl lg:text-4xl font-bold drop-shadow-lg">
          Sistema de gestion de asistencias y nomina semanal
        </h1>
        <p class="text-sm lg:text-xl font-roboto-regular mt-4 drop-shadow-md">
          Olvidate del registro de asistencias en libretas: digitaliza y
          automatiza todos la nomina semanal de tu negocio.
        </p>
      </div>
    </section>

    <!-- ¿QUÉ ES? -->
    <section class="bg-gray-100 py-16 px-6 md:px-20 text-center text-gray-700">
      <h2 class="text-3xl font-bold mb-6">
        ¿Qué es el sistema de asistencias y nómina?
      </h2>
      <p class="max-w-4xl mx-auto text-lg">
        Es una solución creada por Neurovix que permite registrar las
        asistencias diarias del personal y calcular automáticamente la nómina
        semanal. Ideal para negocios como tiendas, talleres o pequeños comercios
        que desean dejar atrás el control en libretas y hojas de Excel.
      </p>
    </section>

    <!-- BENEFICIOS -->
    <section class="bg-white py-16 px-6 md:px-20">
      <h2 class="text-3xl font-bold text-center mb-10">
        Beneficios para Super Market ML
      </h2>
      <div
        class="grid md:grid-cols-2 lg:grid-cols-3 gap-6 text-sm text-gray-700"
      >
        <div class="bg-blue-100 p-6 rounded-xl shadow">
          ✅ Registro diario de asistencias en segundos
        </div>
        <div class="bg-blue-100 p-6 rounded-xl shadow">
          ✅ Cálculo automático de nómina semanal
        </div>
        <div class="bg-blue-100 p-6 rounded-xl shadow">
          ✅ Control de retardos y faltas con reportes visuales
        </div>
        <div class="bg-blue-100 p-6 rounded-xl shadow">
          ✅ Acceso desde cualquier celular o computadora
        </div>
        <div class="bg-blue-100 p-6 rounded-xl shadow">
          ✅ Seguridad de datos y respaldo en la nube
        </div>
        <div class="bg-blue-100 p-6 rounded-xl shadow">
          ✅ Reducción del tiempo administrativo en más de 70%
        </div>
      </div>
    </section>

    <!-- PASOS -->
    <section class="bg-gray-50 py-20 px-6 md:px-20">
      <h2 class="text-3xl font-bold text-center mb-10">¿Cómo funciona?</h2>
      <div class="grid md:grid-cols-5 gap-6 text-center text-sm text-gray-700">
        <div class="flex flex-col items-center">
          <div
            class="w-12 h-12 bg-blue-600 text-white rounded-full flex items-center justify-center text-lg font-bold mb-3 shadow-md"
          >
            1
          </div>
          <p>El empleado registra su entrada o salida</p>
        </div>
        <div class="flex flex-col items-center">
          <div
            class="w-12 h-12 bg-blue-600 text-white rounded-full flex items-center justify-center text-lg font-bold mb-3 shadow-md"
          >
            2
          </div>
          <p>El sistema guarda la asistencia en tiempo real</p>
        </div>
        <div class="flex flex-col items-center">
          <div
            class="w-12 h-12 bg-blue-600 text-white rounded-full flex items-center justify-center text-lg font-bold mb-3 shadow-md"
          >
            3
          </div>
          <p>Cada semana se genera automáticamente la nómina</p>
        </div>
        <div class="flex flex-col items-center">
          <div
            class="w-12 h-12 bg-blue-600 text-white rounded-full flex items-center justify-center text-lg font-bold mb-3 shadow-md"
          >
            4
          </div>
          <p>Se pueden revisar reportes de asistencia y pagos</p>
        </div>
        <div class="flex flex-col items-center">
          <div
            class="w-12 h-12 bg-blue-600 text-white rounded-full flex items-center justify-center text-lg font-bold mb-3 shadow-md"
          >
            5
          </div>
          <p>Todo queda almacenado y accesible en cualquier momento</p>
        </div>
      </div>
    </section>

    <!-- MOCKUP SISTEMA -->
    <section class="bg-white py-20 px-6 md:px-20 w-full text-center">
      <h2 class="text-3xl font-bold mb-6">Así luce por dentro</h2>
      <div class="flex flex-col md:flex-row gap-6 justify-center items-center">
        <Image
          src={MockupSistema1}
          class="rounded-xl w-full md:w-1/2 max-w-md"
          alt="Mockup del sistema"
        />
        <Image
          src={MockupSistema2}
          class="rounded-xl w-full md:w-1/2 max-w-md"
          alt="Mockup del sistema"
        />
      </div>
    </section>

    <!-- TESTIMONIOS -->
    <section class="bg-gray-100 py-20 px-6 md:px-20 text-center">
      <h2 class="text-3xl font-bold mb-6">Lo que dicen nuestros clientes</h2>
      <blockquote class="max-w-3xl mx-auto text-lg italic text-gray-700">
        “Antes llevábamos todo en hojas y cuadernos. Ahora con el sistema de
        Neurovix, el control de asistencias y la nómina es rápido, claro y sin
        errores. Literalmente me ahorra horas los dias de pago de nomina el cual
        nos tomaba de 2 a 3 horas de estres.”
      </blockquote>
      <p class="mt-4 text-sm text-gray-500">
        Sr. Felipe Rios - Dueño de Super Market ML
      </p>
    </section>

    <!-- CTA -->
    <section class="bg-blue-800 text-white text-center py-20 px-6 md:px-20">
      <h2 class="text-4xl font-bold mb-4">
        ¿Listo para automatizar tus asistencias?
      </h2>
      <p class="text-lg mb-6">
        Solicita una demo personalizada y descubre cómo podemos ayudarte.
      </p>
      <a
        href="/contacto"
        class="bg-white text-blue-800 font-bold px-6 py-3 rounded-lg hover:bg-gray-100 transition"
      >
        Solicitar cotizacion
      </a>
    </section>

    <Footer />
  </main>
</Layout>
